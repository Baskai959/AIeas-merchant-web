- [x] 业务导航已从演示模板切换为商家端业务导航，且默认入口符合拍卖后台场景
- [x] 登录、获取当前用户、令牌刷新、登出链路均已按 OpenAPI 接口实现
- [x] HTTP 客户端已统一处理鉴权、错误提示、请求状态与响应解包
- [x] 全局业务状态已以 Zustand 为核心，不再依赖模板 Redux 承担核心业务流转
- [x] 商品创建表单字段与 `ItemCreateRequest` 对齐，商品列表支持筛选与分页
- [x] 拍品创建表单字段与 `AuctionCreateRequest` 对齐，并支持 `startPrice=0`
- [x] 拍品规则编辑仅允许未开拍状态修改，竞拍中及结束态为只读
- [x] 开拍、取消、落锤操作均携带 `Idempotency-Key` 并具备确认交互
- [x] 实时控场页已整合详情接口、实时状态接口与 WebSocket 订阅
- [x] 控场页可展示当前价、领先用户、TopN、倒计时、anti-sniping 提示与连接状态
- [x] 断线重连后可重新订阅房间并刷新状态快照，不依赖事件重放
- [x] 订单列表与详情页可展示成交价、中标用户、保证金、订单状态和支付状态
- [x] 操作日志页面可展示审计日志，或在无权限/无接口时给出明确降级提示
- [x] 核心页面具备加载、空态、错误态、无权限提示，异常情况下不白屏
- [x] 主链路已完成验证：登录 -> 商品创建 -> 拍品创建 -> 开拍 -> 控场 -> 落锤 -> 查单
