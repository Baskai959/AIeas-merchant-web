- [x] 商家端菜单新增「直播间」入口，可创建、编辑、删除直播间
- [x] 直播间列表展示状态徽标（OFFLINE/LIVE/CLOSED）与 `activeAuctionId`
- [x] 「开播」可将直播间状态切换为 `LIVE`，「下播」会先 deactivate 再回到 `OFFLINE`，关键操作携带 `Idempotency-Key`
- [x] 直播间工作台拆分「直播商品 / 待上架商品」Tab，并对接对应 API
- [x] 「上架到直播间」与「下架」分别走挂载与移除接口（含后端待补接口降级提示）
- [x] 「开拍」按钮在房间 `OFFLINE` 或已有在拍商品时被禁用，并附带说明文案
- [x] 同一直播间任意时刻最多只有一个拍品 `RUNNING`，其余 `READY` 卡片的开拍按钮自动置灰
- [x] 取消讲解 / 结束当前拍调用 `deactivate`，成功后房间 `activeAuctionId` 重置
- [x] 商品卡片样式与参考图一致：序号、图片+状态徽标、标题、ID、标签、卖点提示、5 列信息、底部按钮组
- [x] 商品图片加载失败或为空时自动显示默认占位图，且不会无限重试
- [x] 控场页改为 `/live-rooms/:id/control`，按 `activeAuctionId` 路由到对应拍品视图
- [x] 控场页订阅 `/ws/live-rooms/{room_id}`，房间无在拍品时显示「等待开拍」空态
- [x] 拍品列表不再展示「开拍 / 取消 / 落锤」按钮，改为提供「挂入直播间 / 移出直播间」入口
- [x] 后端待补接口（挂载 / 移除直播间拍品）已记录在 spec.md 的「后端待补接口」章节
- [x] `npm run typecheck` 与 `npm run build` 均通过，无新增白屏或路由错误
